import Link from 'next/link';
import PageShell from '@/components/page-shell';
import RelatedLinks from '@/components/related-links';
import { works } from '@/lib/content';
export const metadata={title:'Our Work'};
export default function Work(){return <PageShell><header className="editorial-heading"><p className="eyebrow">Team management & events</p><h1 className="red-text">On the field.<br/>Across borders.</h1><p className="lead-copy">A championship campaign in Sweden. A football tour in India. A cricket event built around unity. Explore the projects that bring NSG’s work to life.</p></header><div className="project-grid">{works.map((w,i)=><Link href={`/work/${w.slug}/`} className={`project-card project-${i}`} key={w.slug} data-reveal><div className={`project-image ${!w.images.length?'logo-project':''}`}><img src={`/images/${w.images[0]||w.logo}`} alt={w.images.length?w.captions[0]:`${w.title} logo`} loading="lazy"/><span className="project-status">{w.status}</span><span className="project-arrow" aria-hidden="true">↗</span></div><div className="project-caption"><p className="eyebrow">{w.category}</p><h2>{w.title}</h2><p className="work-meta">{w.date} · {w.location}</p><p className="body-copy">{w.description}</p></div></Link>)}</div><section className="roster-note" data-reveal><p className="eyebrow">Our work in context</p><h2>People, teams<br/>and the occasion.</h2><p className="body-copy">Every project has a different starting point. Team management places the collective at the centre. Tours focus on a sporting personality and an audience. A cricket tournament brings several teams into a shared competition. Together, they reflect the breadth of Ness Sport Global.</p></section><RelatedLinks links={[{label:'For teams',href:'/expertise/teams/'},{label:'For events',href:'/expertise/events/'}]}/></PageShell>;}
